query getRunners(
  $endCursor: String
) {
  runners(after: $endCursor) {
    nodes {
        ...RunnerReferenceFields
        ...RunnerFieldsCore
    }
    pageInfo {
        ...pageFields
    }
  }
}
